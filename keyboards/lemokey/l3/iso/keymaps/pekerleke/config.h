/* Keymap config: pekerleke (Lemokey L3 ISO)
 *
 * SPDX-License-Identifier: GPL-2.0-or-later
 */

#pragma once

/* First boot after flashing / EEPROM reset: use custom effect (cyclable via RGB_MOD). */
#define RGB_MATRIX_DEFAULT_MODE RGB_MATRIX_CUSTOM_pek_fn_row
